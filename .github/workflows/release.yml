name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    name: Build and publish release assets
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5

      - name: Setup Go
        uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6
        with:
          go-version-file: go.mod
          cache: true

      - name: Build archives
        env:
          VERSION: ${{ github.ref_name }}
        run: |
          set -euo pipefail

          APP_NAME="aws-go-forward"
          DIST_DIR="dist"
          mkdir -p "${DIST_DIR}"

          targets=(
            "darwin amd64"
            "darwin arm64"
            "linux amd64"
            "linux arm64"
            "windows amd64"
            "windows arm64"
            "freebsd amd64"
            "freebsd arm64"
          )

          for target in "${targets[@]}"; do
            set -- ${target}
            goos="$1"
            goarch="$2"

            ext=""
            archive_ext=".tar.gz"
            if [ "${goos}" = "windows" ]; then
              ext=".exe"
              archive_ext=".zip"
            fi

            base="${APP_NAME}-${VERSION}-${goos}-${goarch}"
            pkg_path="${DIST_DIR}/${base}${archive_ext}"
            stage_dir="${DIST_DIR}/${base}"
            bin_name="${APP_NAME}${ext}"
            bin_path="${stage_dir}/${bin_name}"
            readme_path="${stage_dir}/README.md"

            echo "Building ${goos}/${goarch}..."
            mkdir -p "${stage_dir}"
            CGO_ENABLED=0 GOOS="${goos}" GOARCH="${goarch}" \
              go build -trimpath -ldflags="-s -w" -o "${bin_path}" .

            {
              echo "# aws-go-forward (${VERSION})"
              echo
              echo "Target: ${goos}/${goarch}"
              echo
              echo "Archive contents:"
              echo "- ${bin_name}"
              echo "- README.md"
              echo
              echo "Install:"
              echo "1. Extract this archive."
              echo "2. Move the binary to a directory on your PATH."
              echo "3. Ensure executable permissions on Unix-like systems."
              echo
              echo "Examples:"
              echo "- Linux/macOS (system-wide): sudo install -m 0755 ${bin_name} /usr/local/bin/aws-go-forward"
              echo "- Linux/macOS (user-local): install -m 0755 ${bin_name} \"\$HOME/.local/bin/aws-go-forward\""
              echo "- Windows: place ${bin_name} in a directory listed in PATH"
              echo
              echo "Verify:"
              echo "- aws-go-forward --help"
            } > "${readme_path}"

            if [ "${archive_ext}" = ".zip" ]; then
              zip -j "${pkg_path}" "${bin_path}" "${readme_path}"
            else
              tar -czf "${pkg_path}" -C "${stage_dir}" "${bin_name}" "README.md"
            fi

            rm -rf "${stage_dir}"
          done

          (
            cd "${DIST_DIR}"
            sha256sum *.tar.gz *.zip > checksums.txt
          )

      - name: Publish GitHub release
        uses: softprops/action-gh-release@b4309332981a82ec1c5618f44dd2e27cc8bfbfda # v3
        with:
          body: |
            ## Download

            1. Open the **Assets** section below.
            2. Download the archive for your platform:
               - `aws-go-forward-<version>-<os>-<arch>.tar.gz` (Linux/macOS/FreeBSD)
               - `aws-go-forward-<version>-<os>-<arch>.zip` (Windows)
            3. Optionally verify checksums with `checksums.txt`.

            ## Install

            1. Extract the downloaded archive.
            2. Move the binary (`aws-go-forward` or `aws-go-forward.exe`) into a directory on your `PATH`.

            Example (Linux/macOS):
            ```bash
            tar -xzf aws-go-forward-<version>-linux-amd64.tar.gz
            sudo install -m 0755 aws-go-forward /usr/local/bin/aws-go-forward
            aws-go-forward --help
            ```

            Each archive includes a `README.md` with target-specific install notes.
          files: |
            dist/*.tar.gz
            dist/*.zip
            dist/checksums.txt
